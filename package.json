{
  "name": "codex-telegram-relay",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "setup": "bun run src/setup.ts",
    "start": "sh -c 'bun run src/setup.ts && bun run src/relay.ts'",
    "dev": "sh -c 'OKA_WORKSPACE_DIR=.oka bun run src/setup.ts && OKA_WORKSPACE_DIR=.oka bun run --watch src/relay.ts'",
    "voice": "bun run src/voice-relay.ts",
    "voice:call": "bun run src/voice-relay.ts call"
  },
  "dependencies": {
    "grammy": "^1.21.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
