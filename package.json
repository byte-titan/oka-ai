{
  "name": "codex-telegram-relay",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "sh -c 'bun run src/relay.ts & HEARTBEAT_LOOP=true bun run examples/smart-checkin.ts; wait'",
    "dev": "sh -c 'OKA_WORKSPACE_DIR=.oka bun run --watch src/relay.ts & OKA_WORKSPACE_DIR=.oka HEARTBEAT_LOOP=true HEARTBEAT_INTERVAL_MINUTES=5 bun run examples/smart-checkin.ts; wait'",
    "voice": "bun run src/voice-relay.ts",
    "voice:call": "bun run src/voice-relay.ts call"
  },
  "dependencies": {
    "grammy": "^1.21.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
