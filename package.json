{
  "name": "codex-telegram-relay",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/relay.ts",
    "dev": "OKA_WORKSPACE_DIR=.oka bun run --watch src/relay.ts",
    "voice": "bun run src/voice-relay.ts",
    "voice:call": "bun run src/voice-relay.ts call"
  },
  "dependencies": {
    "grammy": "^1.21.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
