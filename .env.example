# Required
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_USER_ID=your_telegram_user_id

# Optional - Codex runtime defaults
# CODEX_PATH=codex                      # Path to codex CLI if not in PATH
# CODEX_REASONING_EFFORT=low            # low | medium | high
# CODEX_FULL_ACCESS=true                # true => bypass sandbox + approvals
# CODEX_SANDBOX=danger-full-access      # Used only when CODEX_FULL_ACCESS=false
# CODEX_MODEL=                          # Optional model override

# Optional - Workspace + prompt
# OKA_WORKSPACE_DIR=~/.oka              # Workspace root for temp/uploads/session/config
# AGENTS_FILE=~/.oka/AGENTS.md          # Main relay prompt file
# HEARTBEAT_FILE=~/.oka/HEARTBEAT.md    # Heartbeat prompt file used by built-in scheduler
# PROMPT_FILE=~/.oka/AGENTS.md          # Backward-compatible alias for AGENTS_FILE
# RELAY_DIR=~/.oka                      # Backward-compatible alias for OKA_WORKSPACE_DIR
# HEARTBEAT_ENABLED=true                # Enable periodic heartbeat scheduler in relay
# HEARTBEAT_INTERVAL_MINUTES=30         # Run heartbeat every X minutes
# HEARTBEAT_RUN_ON_START=true           # Run one heartbeat check when relay starts

# Optional - Voice (if you want voice message support)
# GEMINI_API_KEY=                       # For voice transcription
# ELEVENLABS_API_KEY=                   # For voice responses
# ELEVENLABS_VOICE_ID=                  # Your preferred voice

# Optional - Telnyx + ElevenLabs phone calls (src/voice-relay.ts)
# VOICE_RELAY_ENABLED=true             # Force enable voice relay in main app
# VOICE_RELAY_HOST=0.0.0.0
# VOICE_RELAY_PORT=8787
# VOICE_RELAY_PUBLIC_BASE_URL=          # Public HTTPS URL for Telnyx webhooks (optional)
# VOICE_RELAY_AUTH_TOKEN=               # Optional bearer token for /telnyx/outbound/start
# TELNYX_API_KEY=
# TELNYX_CONNECTION_ID=                 # Telnyx Call Control connection id
# TELNYX_FROM_NUMBER=                   # Your Telnyx number in E.164 (+1...)

# Optional - Memory persistence (if using Supabase)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
